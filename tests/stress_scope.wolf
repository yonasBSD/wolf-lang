print "Scope Hell Starting..."

let int x = 0
let int target = 10000

while x < target
    
    let int y = x * 2
    let int z = y + 5
    let string cop = "this needs to be deleted on memory"
    
    x = x + 1
end

print "Done! if RAM usage not made in it's limits, Garbage Collection (Rust RAII) works."